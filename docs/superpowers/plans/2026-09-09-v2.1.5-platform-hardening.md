# Chat On Steroids 2.1.5 Current Upstream Platform Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port small high-value upstream platform, localization and protocol fixes that remain missing after the feature subsystems land.

**Architecture:** Each fix is independently testable and can be accepted/rejected without coupling to the larger feature ports. Production code changes are minimal manual ports from the cited upstream commits.

**Tech Stack:** TypeScript/JavaScript, Electron, Chrome extension, MCP, Node process discovery, packaging tests.

**Spec:** `docs/superpowers/specs/2026-09-09-upstream-feature-port-v2.1.5-design.md`

---

### Task 1: Multilingual access/rate-limit classification (`6b2c23d`)

**Files:** `extension/content.js`, `src/main/bridge.ts`, `src/main/session/recorder.ts`; tests `test/content-script.test.ts`, `test/bridge.test.ts`

- [ ] Add RED fixtures for non-English access-limit/rate-limit messages represented by upstream tests; assert classifier outcome, not English display copy.
- [ ] Port the classifier invariant without weakening scoped recovery.
- [ ] Run full content-script suite and commit `fix(extension): honor multilingual access-limit verdicts`.

---

### Task 2: RTL answer rendering (`10c25aa`)

**Files:** `src/renderer/chat.ts`, `src/renderer/styles.css`; test `test/renderer-html.test.ts`

- [ ] Add RED Arabic and Hebrew answer fixtures; require the answer body to use its own direction while app controls/tool metadata keep their UI direction.
- [ ] Port bounded `dir`/CSS behavior; do not infer security/identity from text direction.
- [ ] Run content-script/extension tests and commit `fix(extension): render RTL answers in their own direction`.

---

### Task 3: Standard Windows `uv` discovery and modern MCP JSON responses (`53d5aaa`)

**Files:** `src/main/plugins/installer.ts`, `docs/plugins.md`; tests `test/plugins-environment.test.ts`, `test/mcp.test.ts`

- [ ] Add RED Windows candidate tests covering the standard per-user/system `uv.exe` install locations asserted by upstream `test/plugins-environment.test.ts`.
- [ ] Add the upstream #148 MCP JSON fixture to `test/mcp.test.ts` and require the currently supported MCP stack to normalize it without breaking the older envelope. If the test is already green with no production change, classify the protocol half `TEST_ONLY_PORT` rather than changing parser code.
- [ ] Port the `uv` discovery logic into `src/main/plugins/installer.ts`, update `docs/plugins.md` with the actual supported Windows discovery paths, and run both focused tests.
- [ ] Commit `fix(runtime): discover uv installs and accept modern MCP JSON`.

---

### Task 4: Windows swapped mouse buttons (`87a9d9b`)

**Files:** `src/main/computer/helper.ts`; test `test/computer-swapped-buttons.test.ts`

- [ ] Run existing swapped-button tests. If already present and green, classify `ALREADY_PORTED` and do not edit production code.
- [ ] Only port missing platform query/translation behavior behind a failing regression.

---

### Task 5: Brave support and readiness confirmation (`335cb29`, `76b169e`)

**Files:** `src/main/browser.ts`, `src/renderer/index.html`, `src/shared/types.ts`, `src/main/tunnel/index.ts`; tests `test/browser-selection.test.ts`, `test/tunnel-lifecycle.test.ts`

- [ ] Re-run after browser/model plan. If Brave and readiness replacement behavior are already covered, classify `ALREADY_PORTED`.
- [ ] Otherwise add RED and minimal manual port.

---

### Task 6: Packaging/test determinism fixes (`bfd8c98` and current upstream test-only hardening)

**Files:** `.github/workflows/release.yml`, `test/packaging.test.ts`.

- [ ] Reproduce the relevant Linux DEB teardown race or test synchronization issue in the fork before changing anything.
- [ ] Port only test/runtime cleanup behavior that applies to the fork package scripts; do not delete fork Windows signing/ACL/version gates.
- [ ] Run `npx vitest run test/packaging.test.ts` and platform-specific smoke tests available on the current host.
