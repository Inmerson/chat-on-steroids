# Upstream 2.0.9 Selective Port Final Integration Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the independently verified upstream 2.0.9 capability ports on top of fork 2.2.0, prove that every upstream capability is classified, run the protected verification/build/package gates, and produce an unpublished multi-platform release candidate without weakening the official signing gate.

**Architecture:** The fork remains the product architecture. Integration accepts only capability-level changes that preserve the current Core/Desktop/Steromi/Plugins surfaces, Persistent Core, Agent System 3.0, durable execution, multi-device and current release pipeline. The final capability matrix is the audit trail connecting upstream 2.0.9 to `PORT`, `ALREADY_PRESENT`, `SUPERSEDED_BY_FORK`, or `EXCLUDE` decisions. Release candidate workflow is evidence only; official publish remains separately blocked until valid Windows publisher credentials exist.

**Tech Stack:** Git, npm/Vitest/TypeScript/electron-vite, electron-builder packaging scripts, GitHub Actions release candidate matrix.

**Spec:** `docs/superpowers/specs/2026-09-11-upstream-2.0.9-selective-port-design.md`

**Component plans:**
- `docs/superpowers/plans/2026-09-11-upstream-2.0.9-coding-runtime.md`
- `docs/superpowers/plans/2026-09-11-upstream-2.0.9-windows-computer-use.md`
- `docs/superpowers/plans/2026-09-11-upstream-2.0.9-browser-bridge-reliability.md`
- `docs/superpowers/plans/2026-09-11-upstream-2.0.9-recorder-session.md`
- `docs/superpowers/plans/2026-09-11-upstream-2.0.9-workspace-ui-i18n.md`

## Global Constraints

- Authoritative repo remains `C:\Users\exprt\Project Inmersion\Inmersion MCP\Chat On Steroids`; no second physical project copy is created.
- Work integrates into `port/upstream-2.0.9`; preserve the archive branch `archive/pre-2.0.9-port-20260911` at `9f6ca4c5013662e4aafaa67411e8a4c58c621a68`.
- Never merge/rebase the whole upstream branch or cherry-pick `c6f2014` wholesale.
- Do not replace current `.github/workflows/release.yml` / `publish.yml` with upstream 2.0.9 versions.
- Do not alter the Windows Authenticode requirement in official `publish.yml`.
- No tag, GitHub Release or official publish in this plan.
- Version remains 2.2.0 during development/candidate validation unless a separately approved release-version task changes it later.

---

### Task 1: Build the final upstream 2.0.9 capability matrix

**Files:**
- Create: `docs/superpowers/audits/2026-09-11-upstream-2.0.9-capability-matrix.md`
- Read-only source: `git diff 53d5aaa..v2.0.9`, `docs/release-notes/v2.0.9.md`, final fork code/tests.

- [ ] **Step 1: Inventory upstream by capability, not by raw file count**

At minimum include rows for:

- Code Mode / declaration cache / QuickJS;
- task-plan persistence and plan UI;
- Codex-derived instructions/provenance;
- background command results;
- Windows desktop overhaul;
- optional macOS Desktop;
- request-stream exact identity (`8d1f5b3`);
- fast Markdown send receipt (`900be18` + `ffebd99` fixture correction);
- observation-journal fairness;
- browser input/Stop/command custody;
- Compact & Resume reliability;
- worker reuse and idle tabs;
- recorder scoped queues / attribution repair / final identity;
- prompt/title/redaction/benchmark helpers;
- Simplified Chinese;
- local Projects + project removal/grouping;
- ChatGPT Project successor affinity if retained by browser audit;
- transcript/tool-result UI changes;
- connector refresh reminder;
- Plugins behavior;
- provider usage/cost presentation;
- release workflow/package/release-note changes.

- [ ] **Step 2: Assign exactly one final class to every capability**

Each row uses one of `PORT`, `ALREADY_PRESENT`, `SUPERSEDED_BY_FORK`, `EXCLUDE`. Mixed rows must be split until the classification is unambiguous. Link each `PORT` row to implementation commits/tests; each non-port row names the current fork evidence/reason.

- [ ] **Step 3: Cross-check all upstream changed paths**

Run:

```text
git diff --name-status 53d5aaa..v2.0.9
```

Every functional changed path must map to at least one matrix row. Pure docs/release metadata may share a `release/docs` row, but no production/test file is left “unreviewed”.

- [ ] **Step 4: Verify/commit matrix**

Run `git diff --check`, then commit: `docs: classify upstream 2.0.9 capabilities`.

### Task 2: Integrate component tips in dependency order and inspect every conflict

**Files:**
- Git history only unless conflict resolution is needed.

**Order:**
1. coding-runtime legal/principal/declarations;
2. Code Mode + task plans + instructions + background exec;
3. Windows backend/facade;
4. browser/bridge reliability;
5. recorder/session deltas;
6. workspace/i18n/projects/UX.

- [ ] **Step 1: Confirm each component checkpoint has its own focused green evidence**

Do not integrate a worker/branch solely because it reports “done”. Inspect its diff and the exact test output.

- [ ] **Step 2: Integrate one component at a time**

After each merge/cherry-pick of fork-owned implementation commits, run that component's focused union. Resolve conflicts by current 2.2.0 authority, never by accepting an upstream whole-file version.

- [ ] **Step 3: Recheck cross-cutting schemas after every integration**

Inspect `src/shared/types.ts`, `src/shared/session.ts`, `src/main/mcp/surfaces.ts`, `src/main/mcp/kernel.ts`, `src/main/bridge.ts`, `src/preload/index.ts`, `extension/manifest.json`. One producer/consumer protocol must have one final shape.

- [ ] **Step 4: Keep Git history reviewable**

Do not squash unrelated component commits together during debugging. No force-reset/clean of other work.

### Task 3: Run cross-subsystem regression suites

**Files:**
- Tests only unless a real integration defect is reproduced.

- [ ] **Step 1: MCP + Code Mode + terminal**

```text
npx vitest run test/mcp.test.ts test/mcp-inbound.test.ts test/mcp-tool-declarations.test.ts test/code-mode-runtime.test.ts test/code-mode-mcp.test.ts test/mcp-inflight.test.ts test/mcp-shutdown.test.ts test/exec-result-delivery.test.ts test/exec-output-budget-mcp.test.ts
```

- [ ] **Step 2: Browser + session identity**

```text
npx vitest run test/usage-observer.test.ts test/content-script.test.ts test/extension.test.ts test/bridge.test.ts test/correlation.test.ts test/continuation.test.ts test/resume.test.ts test/recorder-final-identity.test.ts test/attribution-repair.test.ts
```

- [ ] **Step 3: Agents + durable execution/orchestration**

```text
npx vitest run test/agents.test.ts test/agent-tab-lifecycle.test.ts test/agent-tab-budget.test.ts test/execution.test.ts test/orchestration-recovery.test.ts test/orchestration-store.test.ts test/orchestration-task-state.test.ts test/orchestration-worker-allocation.test.ts test/control-center.test.ts
```

- [ ] **Step 4: Desktop Windows**

Run the complete Desktop family from the Windows Computer Use plan.

- [ ] **Step 5: Renderer/projects/i18n/plugins**

```text
npx vitest run test/renderer-i18n.test.ts test/renderer-layout.test.ts test/renderer-state.test.ts test/renderer-timeline.test.ts test/renderer-html.test.ts test/tool-result.test.ts test/plugin-refresh-reminder.test.ts test/session-list-refresh.test.ts test/core-ui-dispatch.test.ts test/plugins-surface.test.ts test/plugins-manager.test.ts test/plugin-refresh-workflow.test.ts
```

- [ ] **Step 6: Heavy session suite separately**

Run: `npx vitest run test/session.test.ts`

Fix only reproduced integration defects; add a regression before each production fix.

### Task 4: Run the exact protected local verification gate

- [ ] **Step 1: Capture exact HEAD/status before verification**

```text
git rev-parse HEAD
git status --short --branch
```

- [ ] **Step 2: Run protected CI-equivalent gate**

Run: `npm run verify:ci`

Required evidence:
- Electron prewarm succeeds;
- bundled ripgrep checksum/staging succeeds;
- privacy gate passes;
- notices gate passes;
- TypeScript passes;
- main Vitest pool passes;
- isolated `test/session.test.ts` passes;
- isolated `test/mcp-shutdown.test.ts` passes;
- overall exit code 0.

- [ ] **Step 3: Build and tunnel pin**

```text
npm run build
npm run verify:tunnel-current
git diff --check
git status --short --branch
```

All must exit 0; tracked worktree must be clean before candidate dispatch.

### Task 5: Run local Windows packaged-runtime smoke

- [ ] **Step 1: Build unpacked Windows x64**

Run: `npm run dist:dir:x64`

- [ ] **Step 2: Run packaged runtime smoke**

Run: `node scripts/smoke-packaged-runtime.mjs --platform win32 --arch x64`.

- [ ] **Step 3: Verify extension/runtime payload**

Inside packaged resources prove: extension files include the new MAIN-world observer/i18n-independent browser assets; app and extension both report 2.2.0; bridge protocol agrees; QuickJS runtime/native payload required by Code Mode is packaged; tunnel/ripgrep/native dependencies are present.

- [ ] **Step 4: Run Windows desktop smoke**

Run `node scripts/smoke-windows-desktop.mjs` if the Windows plan added it. Use only disposable/test windows.

- [ ] **Step 5: If building an installer locally, inspect but do not call it official**

Run: `npm run dist:x64`. Check:

```powershell
$sig = Get-AuthenticodeSignature -LiteralPath .\release\Chat-On-Steroids-Setup-x64.exe
$sig.Status
```

An unsigned local candidate may be used for testing. `NotSigned` is not a passing official-release signature.

### Task 6: Push the verified port branch and run an unpublished six-platform release candidate

- [ ] **Step 1: Push exact verified HEAD to Inmerson fork**

Use the fork/upstream remote that resolves to `https://github.com/Inmerson/chat-on-steroids.git`. Then prove:

```text
git ls-remote <inmerson-remote> refs/heads/port/upstream-2.0.9
```

Remote hash must equal local `git rev-parse HEAD` exactly.

- [ ] **Step 2: Dispatch candidate with signing requirement disabled only for candidate**

```text
gh workflow run release.yml -R Inmerson/chat-on-steroids --ref port/upstream-2.0.9 -f require_windows_signing=false
```

This is allowed because `.github/workflows/release.yml` is explicitly the non-publishing candidate workflow. Do not modify `publish.yml`.

- [ ] **Step 3: Prove candidate run head SHA**

Read the run metadata and require `headSha` equal the pushed verified commit before interpreting job results.

- [ ] **Step 4: Require all seven jobs**

All must succeed:
- Windows x64
- Windows arm64
- macOS x64
- macOS arm64
- Linux x64
- Linux arm64
- Assemble release candidate

The new Windows Computer Use tests execute only on supported Windows hosts; non-Windows capability projection must remain absent/disabled as designed.

### Task 7: Inspect candidate artifacts and release blockers

- [ ] **Step 1: Require the existing artifact matrix**

Candidate must contain:

```text
Chat-On-Steroids-Setup-x64.exe
Chat-On-Steroids-Setup-arm64.exe
Chat-On-Steroids-macOS-x64.dmg
Chat-On-Steroids-macOS-x64.zip
Chat-On-Steroids-macOS-arm64.dmg
Chat-On-Steroids-macOS-arm64.zip
Chat-On-Steroids-Linux-x64.AppImage
Chat-On-Steroids-Linux-x64.deb
Chat-On-Steroids-Linux-arm64.AppImage
Chat-On-Steroids-Linux-arm64.deb
Chat-On-Steroids-Extension.zip
SHA256SUMS.txt
```

If the current pipeline includes an additional legally required native-sources artifact after QuickJS/native changes, update the matrix deliberately and test the assembler; do not silently omit required source/notices.

- [ ] **Step 2: Verify `SHA256SUMS.txt` against downloaded candidate files**

No checksum mismatch is acceptable.

- [ ] **Step 3: Inspect Windows candidate signature truthfully**

For both EXEs use `Get-AuthenticodeSignature`. Candidate workflow with `require_windows_signing=false` may produce unsigned artifacts; record actual status. Do not infer publisher signing from electron-builder log text.

- [ ] **Step 4: Recheck official signing prerequisites without exposing secrets**

Check GitHub secret names and local certificate availability only. Never print secret values. If a real publisher certificate/password is absent, record **official public release blocked by Windows signing** and stop before tag/publish.

### Task 8: Final acceptance review

- [ ] **Step 1: Read the design acceptance criteria line by line**

For every criterion in `docs/superpowers/specs/2026-09-11-upstream-2.0.9-selective-port-design.md`, attach concrete evidence: commit/test/run/artifact/matrix row.

- [ ] **Step 2: Confirm no duplicate project tree**

Prove the old scratch path does not exist and authoritative repo path does.

- [ ] **Step 3: Confirm no duplicate authority**

Review the final surface/plan/agent/session/desktop/browser architecture: one MCP registration path, one session store, one Agent System 3.0 authority, one durable execution store, one desktop backend, one extension bridge.

- [ ] **Step 4: Stop at release-readiness boundary**

Do not create `v2.2.0` or another tag and do not dispatch `publish.yml` as part of this port task. Official release is a separate decision and still requires the publisher-signing gate to pass.
