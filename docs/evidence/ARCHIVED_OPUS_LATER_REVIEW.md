# Opus later review notes

Private working notes for the next review checkpoint. Do not interrupt the active Claude Code batch for these unless Claude starts heading in a clearly wrong direction.

## Interaction rule

- Do not continuously message Claude while it is implementing.
- Let Claude finish the current coherent batch before sending review feedback.
- Bundle feedback into one message and explicitly ask: **“Do you think this makes sense?”** Let Claude argue back if it disagrees.
- Course-correct immediately only for a clear wrong direction, destructive action, bad architectural assumption, or user-instruction violation.
- **Do not install/reinstall the app after this batch.** Stop after implementation/tests/build-level verification and wait for further user instructions before any installer/system mutation.

## Findings already surfaced

1. Chat layout root cause: right session card has four direct children but inherited a three-row grid. Correct shape is `auto auto minmax(0,1fr) auto` scoped only to the session card.
2. Duplicate `id="agentFilter"` existed in Chat and Activity. Must stay separated and covered by a unique-ID regression test.
3. Permission-list `.tool` CSS collided with timeline `<details class="tool">`, turning expanded tool calls into flex rows and pushing raw arguments/results sideways. Namespace permission selector (`.tools .tool`) or timeline selector so expanded raw content stays inline beneath its own summary.
4. User explicitly wants session recording/write ON from first launch.
5. User explicitly wants default worker count 2, not 3, due to ChatGPT rate-limit pressure.
6. Standalone Compact button should be removed. Header target: one `Compact & resume` primary action plus gear. Settings is not a peer of Timeline/Compaction.
7. Clear session/agent affordance should be direct per row as an `×` with hover tooltip rather than buried global Clear swarm only.
8. macOS/Linux is deeper than builder targets: current tunnel fetch script hardcodes `windows-amd64`, PowerShell unzip/move and `.exe`; computer-use helper is Win32/PowerShell; UI/tool descriptions hardcode Windows; builder is NSIS-only. Do not claim cross-platform parity from only adding dmg/AppImage targets.
9. Extension auto-connect + recording are prerequisites for the intended hub UX. Avoid manual pairing ceremony.
10. Chronological extension UX should converge toward a single interleaved stream of progress/reasoning and actual MCP tool calls, rather than ChatGPT’s stacked generic rows plus a separate shadow timeline.

## Things to review after Claude finishes the current batch

- Did renderer regression coverage actually prove the 4-row session-card structure and unique DOM ids?
- Did CSS namespacing fix the expanded tool-call body without causing permission-list regressions?
- Did agent-filter IDs get updated in both `chat.ts` and `main.ts` plus tests?
- Did Claude accidentally leave the old standalone Compact path visible anywhere?
- Did it implement user-decided defaults (`record=true`, `maxWorkers=2`) rather than only documenting them?
- Did it avoid installer/reinstall actions?
- Ask Claude whether the proposed architecture makes sense and invite disagreement before the next batch.

11. The requested per-agent `× / Clear session` is not a renderer-only change. Current IPC exposes only `swarm:get` and destructive whole-run `swarm:reset`; there is no individual-agent clear/free operation. The existing session-row trash deletes recorded history, which is different semantics. Review should make the UI action map to an explicit backend lifecycle operation instead of reusing delete-recording or whole-swarm reset by accident.

12. Agent-tools UX has a backend semantics conflict to resolve: `buildServer()` intentionally keeps feature tools monotonically exposed (`exposedAgentTools`) and only returns `FEATURE_DISABLED` after multi-agent is switched off, because ChatGPT may cache a tool snapshot. Tobias’s requested main-page toggle says agents off should mean the agent tools are *not exposed*. This needs an explicit strategy (new MCP server/tool-list refresh/reconnect/new-chat contract) rather than only moving the checkbox in the renderer. Also the main-page `toolsOn()` count currently counts capability tools only, not conditional session/agent tools, so surfacing Agents next to the tool count may expose an inconsistent count unless updated.

## Opus review response after P0

Claude finished the P0 renderer batch, ran `npm run verify`, and then stopped without installing/reinstalling. It agreed with the deferred findings but pushed back on two details:

- **Per-agent clear:** agreed it needs backend lifecycle semantics, but stressed that naive deletion of an agent from the map would recreate the exact half-cleared/tab-storm failure the broker was designed to prevent. Worker `×` should terminally free that worker slot while retaining enough identity/tombstone state to make old credentials/bootstrap retries harmless. Prime `×` is a different operation and effectively ends the whole run. Session-row history deletion is a third, separate action and must not be conflated with either.
- **Agent tools toggle:** agreed the current main-page tool count is already inaccurate because it omits session/agent tools. It disagreed with dynamically changing the already-running MCP server’s tool surface. Existing architecture intentionally exposes monotonically during a server lifetime because ChatGPT can cache schemas, including `agent_key` being injected into many schemas. Its proposed safe contract is: toggling Agents changes config now, but the exposed surface changes on the next clean app/server start/reconnect; UI must state that clearly instead of pretending the cached connection changed immediately.

Claude confirmed P0 status: 4-row structure + unique IDs covered by tests, CSS collision fixed, `npm run verify` green. It explicitly said standalone Compact is still present and `record=true` / `maxWorkers=2` are not implemented yet, only documented. It is waiting for the next batch instruction.


## Live ChatGPT DOM/Fiber investigation — 2026-08-16

13. **T-15 is not primarily a selector failure.** I inspected both a chat where relabelling works and the old `Minecraft Recreate HTML` chat where every row still says `Called tool`. Both use the same live shape: `span.group/tool-message` containing `[aria-label="Open tool call list"]`. In the working chat the extension has added `data-clf-call`; in the failing chat the same target is present but unbound. So `span.group/tool-message` / `Open tool call list` still targets the current renderer correctly.

14. **The failing prime chat has no recorder-side calls to match.** Conversation `6a80ec6c-8e9c-83eb-ab14-f4713aa5a319` maps to session `2026-08-15-669ae2b4`. Its meta says 5 events, 0 tool calls, agent `prime`; `events.jsonl` contains only session start + four user messages. The page has seven visible connector rows, but the local session has zero tool entries. Therefore `/activity?conversationId=...` cannot possibly relabel this historical chat from recorder history alone. This explains a major part of the "works in exactly one chat" symptom.

15. **One visible `Called tool` row is NOT necessarily one MCP call.** Clicking a generic row in that failing chat opened ChatGPT's own `TobisComputer — Tool call list` side panel with nine actual calls (`list_resources`, `create_agents`, several more `list_resources`, `agent_status`, etc.). Another generic row in the same assistant turn opened the same multi-call list. Current `planLabels(blocks, calls)` fundamentally models one rendered block ↔ one recorded call; that assumption is false for grouped/collapsed connector rendering. `collapsedSameToolCallCount` also exists in React props. Add regression fixtures for 1 visible block -> N actual calls and multiple visible blocks representing the same grouped list.

16. **Major opportunity: exact tool identity is already present in React Fiber on a collapsed historical row.** The current `chatgpt-dom.js` comment says provider/tool identity only appears after opening the row/side panel; live inspection disproved that. A `span.group/tool-message` owns a `__reactFiber$<random>` property. Walking a few `.return` ancestors exposed props containing `messages`, `allMessages`, `groupedMessagesToRender`, `turn`, `clientThreadId`, and `collapsedSameToolCallCount`. The tool-request message includes e.g. `recipient: "api_tool.call_tool"` and `content.text` JSON with `path: "/TobisComputer/.../agent_status"` plus args. The paired tool-result message exposes `metadata.invoked_resource.resource_uri` and `metadata.invoked_resource.app_name: "TobisComputer"`. The same structure exposed exact `create_agents` and `list_resources` calls, conversation id, turn id and timestamps in the old chat without opening the side panel.

17. **Proposed robust T-15/T-43 path:** use a deliberately narrow MAIN-world helper to inspect only the React Fiber attached to connector rows, parse the tool-request/result pair there, and emit a *sanitized* descriptor to the isolated content script. Emit only provider fingerprint/app name, tool name/path tail, message/call identity, turn/conversation id, timestamp, group/collapse identity/count, and a safe argument summary. Do NOT forward raw bearer tokens, `agent_key`, full tool results, or arbitrary args across the page boundary. Fail closed when the Fiber shape is absent or unexpected. This can make historical relabelling independent of missing recorder entries, distinguish `TobisComputer` from Gmail/Calendar connectors, and correctly understand collapsed groups.

18. **This also changes T-14/T-16 architecture.** ChatGPT's own message model already contains ordered tool-request timestamps and turn grouping. Combine those with observed progress/reasoning timestamps instead of forcing the visible DOM rows into a false one-row-per-call mapping. The goal remains one chronological transcript, but the source of truth for tool identity can be ChatGPT's own sanitized message metadata, while the local recorder remains authoritative for app-side results/durations when available.

19. Security note for the Fiber route: the raw request JSON contains sensitive fields (observed `agent_key` directly) and user-supplied task text. Any MAIN-world extractor must sanitize before emitting. Prefer explicit allowlisted summary generation and secret-key redaction, not `postMessage` of raw React props.

20. **Stronger direct fixture for the collapse bug:** after expanding the first failing row, its own DOM literally became `Called tool … 4 earlier tool calls hidden. Open tool call list`. Its nearest React props had `collapsedSameToolCallCount: 4` plus the current request/result pair (`agent_status`). So this is direct proof that one rendered row can stand for at least five underlying calls, not merely an inference from the side panel. The other visible rows I sampled each exposed their own exact request pair (`send_agent_message`, `run_command`, `computer`, `screenshot`, etc.) with `collapsedSameToolCallCount: 0`. Any relabeller must model a row as `{representative call, hidden preceding calls/count/group}` rather than assuming every call has a DOM row.

21. Implementation note: Chrome MV3 manifest content scripts support `"world": "MAIN"` (Chrome 111+); this extension already requires Chrome 116. So the Fiber helper can be a separate static MAIN-world content script plus the existing ISOLATED script, without adding broad `scripting` permission or exposing a web-accessible script. Keep the MAIN script tiny because Chrome explicitly warns that the host page can access/interfere with MAIN-world code. Treat its emitted metadata as untrusted evidence and validate again in the isolated/backend layers.

22. **P1/default consistency check while current batch is in flight:** `DEFAULT_SESSIONS.record` is now `true`, but `src/renderer/index.html` Recording help still literally says `Off by default.` Update that copy in the same batch or T-22 will ship a contradictory UI.

23. **T-10 surface check:** the session picker currently still has the old trash affordance (`sess-del`, title `Delete this recorded session`) while the new agent rows are getting `×` clear controls. Keep the semantic distinction from the earlier review, but the user's requested direct `×`/`Clear session` affordance on the chat picker is not satisfied merely by adding worker/prime X buttons. Decide deliberately whether the picker X means deleting recorded history or ending a live agent/session; don't silently leave the old trash while marking T-10 done.

24. **Backlog coverage audit:** most old hardening items are now represented by stable TODO ids, but four old notes are still only in `POST_V1.5.1_HARDENING_BACKLOG.md`: (a) the bounded crash window where a completed attributed tool call exists only in RAM before persistence, (b) terminal sender semantics for finished/failed agents, (c) resume success should distinguish `bootstrap typed/opened` from `handoff actually redeemed`, and (d) after app restart attribution grace should consider live browser-companion presence rather than only an empty in-memory conversation map. Do not lose these when the active TODO is eventually treated as the only queue; either explicitly promote them later or mark them intentionally deferred.

25. **Likely T-20 reopen-spam mechanism:** `/closed` is conversation-wide, but `pagehide` is tab/document-local. `background.js` knows the sender tab id (`tabKey(sender)`) for events/bind, yet `HANDLERS.closed(message)` drops sender identity and posts only `{conversationId}`. Main `bridge.ts /closed` then calls `closeConversation(id)`, which deletes the sole `conversations` map entry and `endSession()` immediately. If the same ChatGPT conversation exists in another tab (especially after the earlier duplicate-tab storm), closing/reloading *one* document ends the shared session; the still-open copy's next activity/event reuses the known ended session and logs `session … reopened`. That can repeat indefinitely. Fix the lifecycle at page-instance/tab granularity: only close/end a conversation when the last known page instance is gone, or make close advisory and use liveness expiry. Also clear `conversationSeen` consistently when truly last-closed. Add a regression: same conversation in two tabs, closing/reloading one must not end/reopen the session while the other remains live.

26. **Important correction to T-15's source-of-truth assumption:** the ChatGPT connector list contains calls the local MCP server can *never* record. In the inspected turn, multiple `api_tool.list_resources` calls appear as `Tool call N: list_resources`; those are ChatGPT/api_tool schema-discovery calls, not invocations of a TobisComputer MCP function, so there is no corresponding `recordToolCall` event in the desktop app. The row group also contains real local calls such as `create_agents`. Therefore “reconcile every generic row against recorder calls” is impossible by construction, and extra api_tool discovery rows make the one-for-one/DOM-order matcher drift even when the real local calls *were* recorded. Treat sanitized ChatGPT MAIN-world metadata as the primary identity for *what ChatGPT rendered*; use recorder events only to enrich matching real local invocations with authoritative outcome/duration/summary/result metadata. Discovery/meta calls should still get honest labels from page metadata but must not be fabricated as desktop-recorded MCP calls.

27. **Release identity:** this working tree is now far beyond the already-installed/shipped 1.5.1, while `package.json` and `extension/manifest.json` still say 1.5.1. Before any final build/install, bump app + extension together (and keep bridge protocol/version compatibility explicit) so the user can actually tell source/new extension from the old installed 1.5.1. Do not install yet; this is only a release-boundary note.

28. **New regression from T-22 default=true: `npm run verify` is leaking session directories into the repo root.** Current untracked `2026-08-16-*` folders are test artifacts; each is `Unattributed activity` and contains the broad MCP tool-exercise sequence from `test/mcp.test.ts` (list_roots/read_file/view_image/write_binary_file/process/... ~50 calls at the test timestamp). `test/mcp.test.ts` never initializes a session store/config fixture because recording used to default off; `ctx.sessionTools=false` only hides session tools, it does not stop global `recordToolCall`. With the new default, recorder root is still `''`, so calls persist relative to cwd. Fix the harness explicitly (temp config/session store with recording false, and flush/reset before cleanup) or make uninitialized recorder fail closed instead of writing to cwd. Then remove only these proven test-generated `2026-08-16-*` repo-root artifacts. Add a regression that a full MCP test run leaves no session directories in cwd.

29. **Tool-count mismatch is directly observable, not theoretical:** the running app Home says `Tools ChatGPT can see 32 of 32`, while this ChatGPT connection currently exposes 40 TobisComputer functions. The missing 8 are exactly the 2 session tools + 6 agent tools that `toolsOn()` / `MAX_TOOL_COUNT` omit. Fix this when T-35 moves Agents to Home; otherwise the new toggle will sit beside a count that visibly contradicts the actual connector surface.

30. **Fiber grouping caution for the in-flight T-56 code:** `collapsedSameToolCallCount` means “earlier tool calls hidden”, not “earlier calls to the same tool”. The live row whose representative was `agent_status` had hidden=4, while the native side panel for that turn included heterogeneous earlier calls (`list_resources`, `create_agents`, more `list_resources`, etc.). So `1 + hidden` is a group *cardinality*, but it is not safe to assume the hidden recorder calls are a contiguous same-tool run or even all recorder-backed calls. The current partial `planLabels()` still consumes `hidden` positions from the recorder list, which will drift whenever those hidden slots include api_tool-only discovery/meta calls. If we want exact hidden-call names, derive them from the larger Fiber turn/group structure (e.g. locate representative message id in `allGroupedMessages`/`groupedMessagesToRender` and walk the preceding hidden tool groups) rather than inventing them from recorder position. At minimum, use hidden only for the `+N` disclosure count and match the representative call by page tool/message identity first.

31. **T-21/T-24 source audit:** auto-connect is already mostly present in source: every content script calls `checkStatus()` on startup, `HANDLERS.status()` discovers the app and silently provisions if no token. So T-21 should probably be verified/marked done rather than reimplemented. The expensive part is `call() -> discover()` doing `/hello` even when a cached port exists; each 2s `/activity` poll therefore costs two loopback requests. Also `content.js` still defines `pollCommands()` but nothing calls it; normal command recovery is already singleton service-worker alarm + targeted marked tabs. Optimize cached-port request/retry and remove/dead-code the unused page poll rather than inventing another command-poll singleton.

32. **T-25 must cover both UIs.** Extension popup `unpair()` only clears its local token, so `refresh()->status()` immediately auto-provisions it again. Desktop Chat settings `Disconnect browser` calls main-process `unpair()` (clears the bridge token), but the next extension request gets 401 and current `call()` silently provisions a replacement too. Therefore “Disconnect” is currently non-persistent from *either* side. A durable disabled/revoked intent needs a defined owner/handshake (at minimum extension-local `disabled` for popup; if desktop button promises the same thing, the bridge must communicate revocation intent rather than only rotating the token), or rename the desktop action to `Reset browser credential` instead of claiming disconnection.

33. **In-flight `applyPageLabel()` wording is too strong.** It currently says “This app did not run the call, so it has no result or duration here.” Fiber-only means only “there is no local recorder entry available to enrich this row.” For api_tool-only meta calls (`list_resources`) the app truly did not run it, but for an old TobisComputer `read_file`/`create_agents` row the app may absolutely have run it while recording/extension was off or before this session was observed. User-facing tooltip should say `Named from ChatGPT's own record; no local recorder entry is available for result/duration` rather than inventing execution provenance.

34. **Page-label → recorder-label upgrade cleanup:** partial `applyPageLabel()` adds `data-clf-page` and class `clf-page`, but `applyLabel()` currently never removes either. If the recorder entry arrives later for that same block, the authoritative title/result can replace the text while the row stays at `.clf-page { opacity:.8; font-style:italic }` and still carries stale page-label state. On a recorder-backed bind, explicitly delete `block.dataset.clfPage` and remove `clf-page` before applying normal outcome/tone styling. Add a regression for page-only label first, recorded enrichment later.

35. **Fiber scan cadence/perf:** partial `pullActivity()` now does `await refreshFiber()` on every 2-second activity poll, even when `fresh=[]` and the DOM has not changed. `fiber.js` walks up to 400 connector rows and up to 30 Fiber ancestors per row, so a long historical chat pays that whole scan forever just because the local activity endpoint is polling. Prefer a dirty flag from `watchToolRows`/MutationObserver + initial scan, or at least throttle/cache descriptors by stamped row/message id. Fiber refresh should happen when connector rows/hydration change, not as a mandatory companion to every `/activity` request.

36. **T-23 defense in depth:** client-side singleflight is necessary, but `/pair` itself can be idempotent too. Right now every POST generates a brand-new bridge token and overwrites the previous one, so two Chrome profiles/service-worker races invalidate each other even if each worker is internally singleflight. Since explicit `unpair()` already clears the stored token, `/pair` can safely return the existing token when one exists and only mint on empty storage. That turns concurrent provisioning into the same credential instead of a rotation race; client singleflight still saves redundant requests.

37. **T-18/T-19 constraint before implementing the “second field”:** the bootstrap text still has to become an actual ChatGPT user message somehow or the model never receives `resume_session` / `join_agent` instructions. A second extension-owned panel can replace the *visible machinery*, but it cannot be the transport by itself. Avoid private ChatGPT APIs/client-state mutation. Safer architecture: show the dedicated status/reasoning panel, mark the real composer as `bootstrap-sending` so the inserted wall is visually suppressed, insert + send as quickly as React safely allows, then collapse/hide/replace the resulting bootstrap user bubble in the DOM with a compact “Resume machinery” / “Worker bootstrap” card. The underlying ChatGPT message remains intact for model context/history; the user’s real composer stays visually clean. Tests should distinguish transport content from presentation rather than pretending no user message was sent.

38. **T-54 can probably be resolved without a new backend action.** Re-reading the original product ask: the chat-picker/session row itself should expose an `×` with tooltip **Clear session**, while prime/workers also get their row-level `×`. The picker already has exactly one destructive action, `deleteSession(summary.id)`, behind a trash glyph. So do **not add a second glyph** and do not reinterpret it as “stop recording”; replace that existing trash presentation with `×` / `Clear session` while preserving the same recorded-history deletion semantics. Agent rows keep their lifecycle semantics. This matches the requested surface without conflating the backends.

39. **Correction to review item 31:** I missed the reference-style call at the bottom of `content.js`: `every(COMMAND_MS, pollCommands)` at ~1507. So `/commands` recovery really **is** polled by every open ChatGPT tab every 20s, in addition to the service-worker alarm. T-24’s singleton-poll part is valid. Keep the other half of item 31: cached-port `/hello` before every `/activity` still doubles the hot-path request volume. The fix should centralize/coalesce page nudges in the service worker (or remove page polling now targeted open + alarm exists), not merely delete an apparently unused function.

40. **Activity endpoint data minimization:** `/activity` currently sends `args: event.call.args.text` (up to the recorder inline cap) and the first 4000 chars of `result` into every ChatGPT content script, but current `content.js` never reads `entry.args`, `argsTruncated`, or `result`; it only needs the sanitized `summary`, tool/outcome/duration/agent/turn/seq. Since the user asked for an *arguments summary*, `ActivitySummary.detail` is already that. Drop raw args/result from the browser activity feed unless a concrete UI needs them later. It reduces poll payloads and keeps file contents/command output out of the browser extension world for no loss of current functionality.

41. **DONE (the `load()` half; `provision()` is still open).** `load()` is now singleflight: the in-flight promise is shared, so a second handler awaits the first initialisation instead of running its own and assigning a stale journal over it. The regression asked for is in `test/extension.test.ts` — two `events` messages on a cold worker, the second arriving while the first is still loading, both entries surviving; the fake storage area models read latency because without it the two loads finish in lockstep and the bug does not reproduce. Making `provision()` singleflight (review item 36) is deliberately left for an extension/bridge batch. Original note: **T-23 has a second, more serious singleflight race in `load()`, not just `/pair`.** `loaded` is only set after two async storage reads. On a cold service-worker wake, two tabs can enter `load()` concurrently; both read the same old journal, then handler A can enqueue/persist a new observation before handler B finishes and assigns its stale `live.journal` back over the global `journal`. That silently loses the event A just accepted. Make initialization itself singleflight (`loadPromise`) and have every handler await the same promise before mutating `port/token/journal/opened/settled`. Then separately make `provision()` singleflight. Add a regression with two concurrent `events` messages on a cold worker; both must survive in the persisted journal/drain.

42. **Multi-tab close fix can live cleanly in the service worker.** Background already has `tabKey(sender)` and binds provisional observations per tab; extend that into a persisted `tab -> conversationId` binding. `HANDLERS.bind` records it. `HANDLERS.closed(message, sender)` removes only that tab and POSTs `/closed` **only if no other bound tab still owns the same conversation**. A `chrome.tabs.onRemoved` cleanup can cover crashes/close paths where `pagehide` never reaches us. Persist bindings in `chrome.storage.session` so a service-worker restart does not forget them. Then main `/closed` can truly mean last-page-gone and can safely clear `conversationSeen` immediately (T-41) as well as end the recorder session. This addresses review item 25 without weakening real close semantics into a long TTL guess.

43. **T-17 tooltip wording:** in-flight agent tags say `Run by ${wantedWho}, not by the chat you are reading.` That second clause is generally false: a worker's recorded calls are normally shown in that worker's own conversation/session, and `prime` is the prime chat itself. Keep the useful attribution but make the tooltip simply `Run by worker-1` / `Agent: prime`; do not imply cross-chat execution unless the UI actually merges agents into one transcript later.

44. **Exact remaining T-18 wall mechanism:** `deliverCommand()` still does `CLF_DOM.insertPrompt(boot.text)`, then deliberately `await sleep(500)`, then re-reads the composer to verify the first 80 chars, then calls `send()`. So even with the new stacked `.clf-stage` panel, the entire resume/worker bootstrap is guaranteed to sit visibly in ChatGPT's real composer for at least half a second (and longer if send readiness is slow). The stage panel improves compaction progress but does **not** by itself satisfy “must not dump its payload into the composer.” Keep the real message transport, but visually mask/suppress the composer during that short machinery insertion and immediately send; then compact the resulting machinery user bubble (T-19) without deleting its underlying text/context.

45. **T-19 folding latency / immediate provenance:** current `foldBootstrap()` learns `bootstrap` from `/activity -> summary.origin.kind`, which is durable and great for reloads, but that origin is only recorded after `/commands/ack` knows the new conversation id. So the just-sent bootstrap user bubble can remain a full wall until the next activity poll even though this content script itself just delivered it. Carry a non-secret `purpose: "worker" | "resume"` (or equivalent) in `BridgeCommand`, set a local `bootstrap` hint before/when sending, fold the first user message immediately after it appears, then let the durable `summary.origin.kind` overwrite/confirm it on later polls/reloads. Avoid parsing the bootstrap text or inferring purpose from `agent === null/prime/worker-n` when the command spec already knows exactly what it is.

46. **Disconnect state must propagate into the page control.** The in-flight `HANDLERS.status()` now returns both `connected: found !== null` and `disconnected`, but `content.js checkStatus()` only keeps `{ connected, paired }`, and `controlState()` enables Compact & resume from `connected`, not `paired`. If the user explicitly disconnects, the app is still discoverable so `connected=true`, the page control looks enabled, and pressing it only fails later with `error: disconnected`. Either define status `connected` as usable/paired (`found && token && !disconnected`) and expose a separate `appFound` for the popup, or have content preserve `disconnected`/`paired` and disable the control with a clear “Browser disconnected” hint.
